# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Overview

This is a content repository, not an application. It contains a Chinese-language book, 《道德经81章投资心法》, and its source material. `python3 scripts/build_codex_epub.py` rebuilds and validates the Codex EPUB (Python 3 and Pillow required).

- `chapters/第01章.md` … `第81章.md` — the book: one essay per Tao Te Ching chapter (81 files, zero-padded names)
- `codex/第01章.md` … `第81章.md` — the rewritten Codex narrative edition: exact source text followed by a plain-language `投资心法` and a practical recommendation under 100 Chinese characters; `codex/README.md` is its methodology and index
- `道德经81章投资心法Codex版本.epub` — the generated EPUB 3 edition, including its cover, dual-format navigation, 81 chapters, and the Wang Bi source appendix
- `scripts/build_codex_epub.py` — the reproducible EPUB builder and structural validator
- `普通投资者的努力方向与时间分配.md` / `.pdf` and `专业投资研究指南.md` / `.pdf` — the two guidebooks (nine and seven chapters). Build only these with `python3 scripts/build_pdf.py effort research`. The PDF builder preserves the source Markdown, creates series-style covers and a page-numbered TOC, and validates every source text line plus all heading bookmarks. These guides provide Markdown and PDF; they do not depend on EPUB artifacts.
- `投资三十二条军规.md` / `.epub` / `.pdf` — the condensed discipline edition (`rules32/`): eight groups and exactly 32 rules, each with four operating steps, trigger handling and advice under 100 characters; a fictional 100-unit account walkthrough, five operation cards, and complete provenance. `rules32/data/归并映射.json` assigns all 360 catalog disciplines uniquely and maps 152 supplementary P/R/T/Q-C entries. Editorial adaptations and original parameters are distinguished explicitly. Build with `python3 rules32/assemble_book.py`, validate with `python3 rules32/validate_book.py`, then `python3 scripts/build_system_epub.py rules32` and `python3 scripts/build_pdf.py rules32`; `python3 rules32/validate_book.py --artifacts` also checks full text, EPUB links, PDF bookmarks and case arithmetic. Source hashes require renewed editorial review when upstream books change. See `rules32/BOOK_SPEC.md`.
- `投资心法100条精选版.md` / `投资心法313条全量版.md` — the three-book distillation (this book, the Codex edition, and 第三只眼观): 313 per-chapter 心法 merged into 100 ranked entries, each carrying the verbatim source quotes with attributions. `python3 scripts/build_selection_epub.py` rebuilds and validates `投资心法100条精选版.epub` (9 themed parts, 100 entry pages, 400 quotes; the validator asserts entry numbering, per-part ranges, and quote counts against the Markdown source)
- `jinbing-drdj/第01篇.md` … `第72篇.md` — reading notes on 金冰《人生悟道 渡人渡己·投资篇（纪念版）》 (72 articles in three parts: 基础篇 1–25, 进阶篇 26–48, 哲思篇 49–72). Each file is `## 中心思想` (faithful précis of that article, anchored on its own examples) + `## 投资心法` (600–1500-char independently written essay) + `## 实操建议` (≤100 chars); `jinbing-drdj/README.md` is its index and methodology. `python3 scripts/build_jinbing_epub.py` rebuilds and validates `人生悟道渡人渡己投资篇读书心法.epub`. **The source book is a third-party work: it is gitignored (`jinbing-drdj/*.pdf`) and must not be committed.** Every verbatim source string must sit inside `<mark>` (≤2 per file, ≤30 chars each) and is verified character-by-character against the PDF at build time; any 30-character run shared with the source outside `<mark>` fails the build.
- `投资体系与方法论.md` / `投资体系与方法论·道德经对照版.md` — the operating-system edition: the five source books (this book, the Codex edition, 第三只眼观, 金冰 reading notes, and the 100-entry selection) exhaustively mined and reorganized into a runnable system — a decision loop, nine dimensions (认知/研究/风控/仓位/买卖点/周期/心力/纪律/修炼), twelve tension arbitrations, a calendar-based operations manual, and fourteen copyable forms. Every principle, rule, and number carries a source tag (`道N` / `道N章实操` / `CodexN` / `三眼N` / `金N篇实操` / `选N`); editor-supplied connective reasoning is marked `（综合）`; gaps the corpus does not cover are declared verbatim as 「语料未覆盖」. Machine-verified coverage: 道 81/81, Codex 81/81, 三眼 151/151, 金 72/72, all 234 实操建议, all 100 selection groups.
  - The two files differ **only** by one layer: the 对照版 adds 680 《道德经》 source-text annotations plus 附录D. Annotations use the `▍` marker (`▍《道德经》第二十四章：…` full form for 核心原则; `▍二十四章「企者不立」` compact form for rules) — **Chinese numerals after `▍` mean the 道德经 original chapter, Arabic numerals inside `[…]` mean a chapter of one of the five心法 books**. Every annotated fragment must be a *contiguous verbatim* run from the named chapter of `原文/道德经-王弼本.md`; explicit `……` is the only permitted splice. Blocks that belong only to the 对照版 are wrapped in `<!-- ddj-only -->` markers in the shards, so the plain edition is produced by mechanically stripping annotations — it must rebuild byte-identical.
  - Both editions assembled from shards under the job scratch directory. **Their EPUBs were retired on 2026-09-07** (files deleted, download links removed; the two reading pages stay). The shared builder `scripts/build_system_epub.py` now has only three editions — `playbook`, `catalog`, `system` — and its generic Markdown→XHTML converter still understands the `▍` annotations should the 对照版 ever be rebuilt.
- `道德经投资打法手册.md` — the playbook edition (`playbook/`): the 心法 converted into executable decision rules — eight decision points (论点/证伪清单/估值锚/仓位/入场/加减/回撤与心力/卖出与复盘, one chapter each), a full walk-through of one real trade (第九章, the「案例账户」normalized to 100 units), four business-model templates (半导体与代工/AI 基建与电力/加密金融轨道/企业软件) plus a from-scratch template chapter, options-as-insurance-only, an operating calendar, and a training path; appendices: parameter table + rule registry (A), the 234 实操建议 mapped one by one to book rules (B), verbatim 《道德经》 chapter text for every quoted line (C), blank forms (D). `playbook/BOOK_SPEC.md` is the single source of truth for parameters, rule IDs (`R1.1`…`R17.3`), and the per-chapter 《道德经》 anchors; chapters live in `playbook/book/NN-*.md` and are assembled (H1→H2 demotion) by `python3 playbook/assemble_book.py`; `python3 playbook/gen_appendix_c.py` regenerates appendix C; `python3 playbook/validate_book.py` must pass (every blockquoted 《道德经》 line verbatim in the named chapter of `原文/道德经-王弼本.md`, every `R`-number registered, lengths within spec, no real account figures); `python3 scripts/build_system_epub.py playbook` builds `道德经投资打法手册.epub` (the no-arg form now builds all three editions). Conventions: rule → formula → number; the reader is 「你」, the case is 「案例账户」 (never real amounts); company facts are dated `（YYYY-MM，核对）` and knowledge stops at early 2026 — never narrate price moves; corpus tags only on rules actually derived from the corpus; companies appear only as method illustrations, never as picks. **`playbook/NBIS全程复盘·样张.md` holds the un-normalized sample with real figures and is gitignored — never commit it.**
- `投资纪律总表.md` / `道德经投资系统.md` — the law layer and the machine layer (`system/`). The catalog is rendered by `python3 system/render_catalog.py --render` from `system/data/纪律总表.json`: 360 disciplines (`C01-001`…`C18-022`, 18 categories, four forms 铁律/条件律/门槛/节律), every one with verbatim source quotes that the renderer re-verifies character-by-character against the corpus files (tags 道N/CodexN/三眼N/金N/罗盘N/罗盘建议/选N/全N), 47 conflicts listed but not adjudicated, plus an 原则区 appendix. The system is assembled by `python3 system/assemble_system.py` from `system/book/*.md` (spec: `system/SYSTEM_SPEC.md`): 宪法十二条 (宪一…宪十二), account states S0–S4 and position states P0–P8, four ledgers + 甲/乙/丙 记点, adjudications §4.1–§4.49, audit cadences, three-layer amendment procedure, new rules R18.1–R18.20, nine system forms (appendix D), and a 360-row execution matrix (`system/book/07a–07f`). `python3 system/validate_system.py` must pass (every cited `Cxx-nnn` exists, every `R` rule registered, matrix ids == catalog ids, 道德经 quotes verbatim, grade level consistent with §3.3, audit-table row counts match the interface chapter, no stale wording, no real figures); `python3 scripts/build_system_epub.py system` builds `道德经投资系统.epub`, and `… catalog` builds `投资纪律总表.epub` (62 pages; the builder's `split_h3` gives each of the 18 categories its own page inside 第二部分, 第四部分 and 附录 A, and the validator asserts all 360 ids survive). EPUB CSS must never put `overflow` on tables or `<pre>` — a scroll container is a monolithic box in paginated readers and anything taller than one screen renders as blank pages. `python3 scripts/build_pdf.py` produces A4 PDFs for all ten books — the three rule-layer books, `股票量化投资系统.pdf`, `成功投资者心性养成指南.pdf`, plus `道德经81章投资心法.pdf`, `道德经81章投资心法Codex版本.pdf`, `投资心法100条精选版.pdf`, `人生悟道渡人渡己投资篇读书心法.pdf`, `第三只眼观投资心法.pdf` (each named like its EPUB and reusing that EPUB's cover). Per-chapter books load one file per section (H1 = chapter, `##` demoted, trailing nav links and HTML comments dropped, `<mark>`/`<u>` restored as gold bold / green underline); the 100-entry selection keeps its intro as a 卷首 section; the 渡人渡己 notes get three part pages. Pipeline: Chrome headless lays out the same converter output with print CSS and emits the outline; PyMuPDF adds cover, title page, a TOC whose page numbers come from that outline, running headers (book title / current chapter) and page numbers, page labels, and `subset_fonts()`. Two hard-won rules live in that script: headings use Hiragino Sans GB, not PingFang (Chrome embeds PingFang as per-page Type 3 fonts and the file balloons to ~16 MB), and every page is `wrap_contents()`-ed before stamping (Chrome leaves its CTM open, which otherwise shrinks and displaces the stamped text). Chrome does not exit after printing on macOS — the script kills it once the output file stops growing. Parameters are never redefined here — they come from `playbook/BOOK_SPEC.md` §1; the system only adds ★ items. Conventions: the reader is 「你」; cite disciplines by id, rules by `Rx.y`, constitution by 宪N; the catalog is the only law source and the handbook's R rules are referenced, never rewritten.
- `股票量化投资系统.md` — the code layer: a v1.0 design specification (13 chapters, single file, H1 + a leading blockquote + `##` chapters with `###` sections) for a program that executes the rule layers — data/time contracts, research and three-scenario valuation, position and portfolio constraints, order idempotency, backtest gates, one fictional end-to-end trade. It is a design deliverable, not running code; nothing in it may be read as live-trading authorization. It links to companion documents under `quant/` (contracts, discipline mapping, reading log) — those are **not** part of the published book. EPUB via `python3 scripts/build_system_epub.py quant` (edition `quant`: `intro_title="卷首"` turns the leading blockquote into its own page; the converter renders repo-internal links as plain text since an offline book has nowhere to send them, and the validator rejects any leftover `<a href="/`), PDF via `python3 scripts/build_pdf.py quant` (meta line states the spec-only status). No 道德经 quote floor applies: it cites chapters as `第N章：「…」` rather than `《道德经》第N章`.
- `成功投资者心性养成指南.md` — the person layer (`mindset/`): a one-year training manual for a reader with a gambler's temperament (all-in, repeated wipeouts, "compounding is too slow"), converting the 道德经 temperament into 36 counted practices `T1.1`…`T12.3` (registry and ★ parameters in `mindset/BOOK_SPEC.md`; design record, arithmetic worksheet, literature whitelist, gap report and the eight theme dossiers under `mindset/design/`). Twelve chapters in calendar order (停手期 30 天 → 复训期 90 天 → 规则实盘 → 稳态), relapse and windfall protocols consistent with the 投资系统 states and penalty ledger, a scorecard with no return column, and the 案例账户 walked month by month (one lapse, one relapse, no wipeout). Chapters live in `mindset/book/NN-*.md` and are assembled by `python3 mindset/assemble_book.py` (chapter order read from the spec's chapter table); `python3 mindset/gen_appendices.py` regenerates appendices A and D from the registry, `python3 mindset/gen_appendix_c.py` regenerates appendix C; `python3 mindset/validate_book.py` must pass (道德经 quotes verbatim in both blockquote and inline forms, corpus quotes verbatim in the named source file, `T` ids registered, `R`/`Cxx-nnn`/宪N exist, external citations only from appendix E, lengths, generic PII patterns, no `$` prices). Conventions: the reader is 「你」; 案例账户＝100 单位; psychology and arithmetic are marked 「编者设定／文献（作者，年）」, never attributed to the corpus; the book adds practices only and never rewrites rules or parameters; the operating-account cap follows one formula (承受线 ÷ 假定最大损失率, spec §1.4) and every other ★ parameter can only tighten. EPUB `python3 scripts/build_system_epub.py mind`, PDF `python3 scripts/build_pdf.py mind`.
- `原文/道德经-王弼本.md` — the sole textual standard for all quoted 原文 (王弼通行本, split and numbered from `daodejing.md`; converted from traditional to simplified characters on 2026-08-15 per user request — the traditional original remains in `daodejing.md`; editorial and conversion notes in its header, e.g. 第33章 restoration, 第31章 校勘括号, 閒→间/繟-kept overrides)
- `README.md` — book index: 81-chapter table (core line, one-line essence, link) plus the unified disclaimer
- `验收报告.md` — acceptance report from the multi-agent build (seven criteria, per-group verdicts)
- `compass_artifact_wf-d0d02179-…_text_markdown.md` — the source essay 《道德经》里的投资心法 (100 entries in eight themes) whose insights the chapters absorbed; its Caveats section is the reference for quote attributions and textual variants
- `daodejing.md` — user-supplied raw text (unnumbered chapter markers); treat as read-only input

## Content Conventions

Every original file in `chapters/` follows one fixed format — preserve it when editing:

- H1: `# 第X章 · 核心句` (Chinese-numeral chapter number + the chapter's most resonant line)
- `## 原文`: the full chapter as a blockquote, copied verbatim from `原文/道德经-王弼本.md` — simplified characters (base file is itself a t2s conversion; see its header for the conversion notes), including editorial brackets 〔〕 and small-form punctuation ﹖﹕﹗､, never paraphrased. Any 原文 change must first be made in the base file, with a header 校记 note.
- `## 投资心法`: a 600–1500-character simplified-Chinese essay in the established style: personal/life entry point → at least one concrete market scene (with sensory detail) → 先破后立 → circle back to the original lines → closing antithetical distillation (对仗式心法, e.g. "为学日益为向外求…／为道日损为向内修…"). Chapters on statecraft/war extend naturally into risk, cycles, or governance — never forced. Verbatim 道德经 quotes inside the essay are wrapped in `<mark>…</mark>` (styled bold + gold via `index.html` CSS); additionally, exactly two law-like/summarizing aphorisms per essay are wrapped in `<u>…</u>` (styled as a green underline). Preserve both kinds of tags when editing and apply them to any newly added quote or aphorism the same way. No per-file disclaimer (README carries the single disclaimer).
- `## 实操建议` (added 2026-08-19): one plain-prose paragraph of 1–3 imperative sentences, ≤100 non-whitespace characters, derived from the essay's own scenes and closing 心法 — concrete behavioral discipline (a trigger, number, or checkable rule), no `<mark>`/`<u>` tags, no lists, no investor quotes, no stock/sector tips. It is written fresh per chapter and intentionally does not duplicate the codex edition's 实操建议.

Codex files use `## 原文`, `## 投资心法`, and `## 实操建议`, followed by navigation. The prose standard is: an everyday or personal entry point, a concrete market scene, a plain-language explanation that returns to the original lines, and a memorable closing distillation. Each Codex essay must be independently written rather than adapted from `chapters/`: the EPUB builder compares it with all 81 main-book essays and rejects normalized similarity at or above 30% or reuse of a complete paragraph of at least 50 characters. Keep each advice paragraph below 100 characters and preserve the methodology and disclaimer in `codex/README.md`.

Cross-cutting standards (apply to chapters and the compass essay alike):

- **Attribution rigor**: investor quotes must trace to verified primary sources; uncertain ones are phrased as paraphrase ("芒格说过，大意是……"). Known corrections stay corrected — the "stock forecasters / fortune tellers" line is Buffett (1992 shareholder letter), not Graham; Livermore's "sitting, not thinking" is Lefèvre's literary rendering in *Reminiscences of a Stock Operator*.
- **Empirical claims cite sources with scope limits**: e.g. Barber & Odean (2000, *The Journal of Finance*), 66,465 accounts, 1991–1996 US discount-broker sample — not extrapolated to today's Chinese market.
- **No stock tips**: sectors and episodes (光模块、2015配资牛市 etc.) appear only as illustrations of behavior, never recommendations.
